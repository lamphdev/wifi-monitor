import { ApChartDirective } from './ap-chart.directive';

describe('ApChartDirective', () => {
  it('should create an instance', () => {
    const directive = new ApChartDirective();
    expect(directive).toBeTruthy();
  });
});
